import type { PageContent } from '@/types/site'

/**
 * Components Page Content Data Dictionary.
 * Centralized static configuration and text definitions.
 */
export const componentsPageContent: PageContent = {
  title: 'components.',
  subtitle: 'A collection of reusable UI components and micro-interactions.',
}
