import type { PageContent } from '@/types/site'

/** Copy for the catalog page */
export const catalogPageContent: PageContent = {
  title: 'catalog.',
  subtitle: 'Window shopping is free, but my recommendations are priceless.',
}
