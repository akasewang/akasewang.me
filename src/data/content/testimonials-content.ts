import type { PageContent } from '@/types/site'

/** Copy for the testimonials page */
export const testimonialsPageContent: PageContent = {
  title: 'testimonials.',
  subtitle: "Proof that I'm actually a decent human being (according to some).",
  /** The closing line under the page, shown by the page and by its loading state alike */
  footerText: 'Kind words from people who (mostly) like me.',
}
