/**
 * Copy for the photos page. It carries no heading of its own, the grid being the page, so this is
 * the closing line and nothing else.
 */
export const photosPageContent = {
  /** Shown by the page and by its loading state alike, so the two cannot say different things */
  footerText: "Taking photos so I don't have to remember things.",
}
