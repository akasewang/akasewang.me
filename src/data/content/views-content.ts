import type { ViewsContent } from '@/types/site'

/** Copy for the view counter, including what it says when the count cannot be read */
export const viewsContent: ViewsContent = {
  title: 'insights.',
  offline: 'Offline',
  sessions: 'Sessions',
  views: 'Views',
  visits: 'Visits',
}
