import type { ViewsContent } from '@/types/site'

/**
 * Views Content Data Dictionary.
 * Centralized static configuration and text definitions.
 */
export const viewsContent: ViewsContent = {
  title: 'insights.',
  offline: 'Offline',
  visitors: 'Visitors',
  views: 'Views',
}
