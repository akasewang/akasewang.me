import type { PageContent } from '@/types/site'

/** Copy for the directory of profiles elsewhere */
export const linksPageContent: PageContent = {
  title: 'links.',
  subtitle: 'Everywhere else you can find me online, grouped by what I do there.',
  /** The closing line under the page, shown by the page and by its loading state alike */
  footerText: "That's every corner of the internet where I currently pretend to be active.",
}
