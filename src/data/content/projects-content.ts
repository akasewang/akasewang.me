import type { PageContent } from '@/types/site'

/**
 * Projects Page Content Data Dictionary.
 * Centralized static configuration and text definitions.
 */
export const projectsPageContent: PageContent = {
  title: 'projects.',
  subtitle: 'A showcase of things that (mostly) work as intended.',
}
